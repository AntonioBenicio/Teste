#include <stdio.h>

void troca(int *a, int *b){
int tmp;

int main(void) {

  int Matriz[8][8];
 int *z, C;
  unsigned long long F;

  for (int i = 0; i < 8; i++) {
    printf("\n");
    for (int j = 0; j < 8; j++) {
      Matriz[i][j] = rand() % 2;
      printf("%d", Matriz[i][j]);
    }
  }


  return 0;
}
